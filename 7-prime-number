public Boolean isPrime(Integer num) {
    Boolean resultado = false;

    for(Integer i = 2; i <= num; i++) {
        if((Math.mod(num, i) == 0)) {
            resultado = false;
            break;
        }
    }

   
    return resultado;
    
}
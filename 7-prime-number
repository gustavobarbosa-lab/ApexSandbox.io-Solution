public Boolean isPrime(Integer num) {
    Boolean resultado = false;

    for(Integer i = 1; i < num; i++) {
        if((Math.mod(num, i) == 0)) {
            resultado = true;
            break
        }
    }

   
    return resultado;
    
}
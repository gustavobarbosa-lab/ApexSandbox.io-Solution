public Map<String, String> phonebook(List<Contact> contacts) {
    Map<String, String> resultado = new Map<String, String>();

    for(Contact contato : contacts) {
        if(contato.Phone != null) {
            resultado.put(contato.Name, contato.Phone);
        }
    }

    return resultado;
}
